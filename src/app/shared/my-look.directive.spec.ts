import { MyLookDirective } from './my-look.directive';

describe('MyLookDirective', () => {
  it('should create an instance', () => {
    const directive = new MyLookDirective();
    expect(directive).toBeTruthy();
  });
});
